<%= form_for @course do |c| %>
 <div class="field last">
    <%= c.label :name %>
    <%= c.text_field :name %>
 </div>

 <div class="field">
    <%= c.label :hours %>
    <%= c.number_field :hours %>
 </div>

 <div class="field">
    <%= c.submit %>
 </div>

<% end %>
